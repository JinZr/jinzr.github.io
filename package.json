{
  "name": "jinzr-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/sync-root-output.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build && git diff --exit-code -- index.html site-assets assets icons favicon.webp manifest.json .nojekyll"
  },
  "dependencies": {
    "@material/web": "^2.4.1"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
